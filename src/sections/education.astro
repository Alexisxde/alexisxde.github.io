---
import { education } from "@/cv.json"
import { Icon } from "astro-icon/components"
---

<section class="flex flex-col gap-4 mt-8">
  {
    education.map(({ title, items }) => (
      <h2 class="text-xl font-semibold">{title}</h2>
      <div class="flex flex-col gap-2">
        {
          items.map(({ name, description, source, url }) => (
            <div class="py-4 px-6 gap-4 rounded-md ring-1 ring-inset bg-neutral-600/10 text-neutral-100 ring-neutral-700/30 space-y-1 group">
              <div class="flex items-center justify-between">
                <span class="text-lg font-medium">{source}</span>
                {url && <a href={url} target="_blank">
                  <Icon name="arrow-up" class="group-hover:-translate-y-1 group-hover:translate-x-2 size-4 hover:opacity-75 transition-all duration-300 ease-in-out" />  
                </a>}
              </div>
              <p class="text-xs text-neutral-300">{name}</p>
              <p class="text-pretty text-xs text-neutral-400">{description}</p>
            </div>
          ))
        }
      </div>
    ))
  }
  <p class="px-2 text-center text-[11px] text-neutral-500">También realize cursos de manera autodidacta desde Youtube, Udemy y alguna que otra página de internet.</p>
</section>

---
/* @import components */
import Section from '@/components/Section.astro';

/* @import json */
import { education } from '@cv'

const { institution, startDate, endDate, area, url } = education
const starYear = new Date(startDate).getFullYear()
---

<Section title='Educación'>
  <a 
    href={`${url}`} 
    target='_blank' 
    class='font-semibold text-md mb-1'>
    {institution}
  </a>
  <div class='flex justify-between items-center w-full text-sm'>
    <span>{area}</span>
    <span>{starYear} - {endDate}</span>
  </div>
</Section>
